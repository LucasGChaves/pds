export enum ReactQueryCache {
  PETS = "pets",
  VETS = "vets",
  VACCINES = "vaccines",
  APPOINTMENTS = "appointments",
  PET = "pet",
  VET = "vet",
  APPOINTMENT = "appointment",
  LOGGED_USER = "loggedUser",
}

export const STALE_TIME = 1000 * 60 * 60;
